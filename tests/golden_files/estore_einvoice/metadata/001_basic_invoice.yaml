test_id: "001"
description: basic_invoice
format: estore_einvoice
parameters:
  convert_to_format: estore_einvoice
  estore_store_number: ""
  estore_Vendor_OId: ""
  estore_vendor_NameVendorOID: ""
# NOTE: estore_einvoice does NOT use InvFetcher (no DB lookup is required
# — header fields are built from the EDI A record + parameters). The
# test harness's ``test_inv_fetcher_data`` key is intentionally omitted
# here; including it would trigger the mock-invocation assertion.
